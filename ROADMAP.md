# ROADMAP

## 文档目的

本文只写从当前仓库现状走向目标架构的待完成事项，不重复描述已经完成的模块。  
每项任务都包含优先级和验收条件。

优先级定义：

- `P0`：必须完成，否则目标架构不成立
- `P1`：建议完成，能显著提升可用性和说服力
- `P2`：后续增强，不影响第一阶段落地

---

## P0

### 1. 检索底座从 Elasticsearch 迁移到 Milvus + BGE-M3（已完成）

**目标**

- 用 Milvus 替换当前 ES 主检索链路
- BGE-M3 只负责 dense embedding
- Milvus 全文/BM25 负责精确 Token 召回
- RRF 融合两路结果

**原因**

- 这是目标架构的核心前提
- 不完成这一步，现状和目标仍然是两套口径

**验收条件**

- Java 检索主链路不再依赖 `UserKnowledgeElasticsearchSearcher` / `PublicKnowledgeElasticsearchSearcher`
- Python Worker 不再向 ES 主知识索引写入文档切片
- 至少验证以下样例并记录结果：
  - `order-service`
  - `ECONNRESET`
  - `/api/v1/order/create`
  - `spring.redis.timeout`
- 至少完成一组 `dense-only vs hybrid` 对照评测

### 2. 新增 Query Router，拆分简单问题与复杂问题（已完成）

**目标**

- 简单 SOP/文档问题走 Hybrid RAG 快速路径
- 复杂故障问题走受限 Agent 路径

**原因**

- 当前系统所有对话统一走 Agent，不符合目标边界
- 这是“为什么需要 Agent”的核心答辩点

**验收条件**

- 入口存在明确 Router 模块或等价服务
- 至少有两类测试样例：
  - SOP/Runbook 问答不进入 Agent
  - 状态+日志联合排查进入 Agent
- Trace 中能看到请求被分配到哪条路径

### 3. 落地排障专用工具集（已完成）

**目标**

- `knowledge_search_tool`
- `service_status_tool`
- `log_search_tool`

**原因**

- 当前工具集仍然是通用搜索、文件、天气、邮件工具
- 不足以支撑“排障辅助 Agent”的定位

**验收条件**

- 三个工具完成注册、参数 schema、返回 schema
- 三个工具都具备：
  - 时间范围限制
  - 返回条数限制
  - 超时
  - 审计记录
- Agent 能基于至少一组模拟故障数据完成多步排查

### 4. 权限模型升级为 `workspace + visibility`（已完成）

**目标**

- 从 `user_id + scope_type` 演进到：
  - `workspace_id`
  - `owner_user_id`
  - `visibility = PRIVATE | WORKSPACE`

**原因**

- 当前纯用户隔离不符合团队知识共享场景
- 目标架构必须支持团队级 Runbook 和复盘沉淀

**验收条件**

- MySQL 核心实体完成字段升级
- 向量库 metadata 完成字段升级
- 服务端统一注入 `workspaceId`
- 存在越权测试，验证跨工作空间召回数为 0

### 5. 统一证据模型与结构化回答（已完成）

**目标**

- 引入 `Evidence Assembler`
- 模型先输出结构化对象，再由后端渲染
- 引用通过 `Evidence ID` 绑定，而不是自由文本

**原因**

- 当前回答结构仍偏自由文本
- 目标架构要能证明“每个结论都能追到证据”

**验收条件**

- 后端存在 Evidence 数据结构与分配逻辑
- 回答对象至少包含：
  - `judgement`
  - `possibleCauses`
  - `steps`
  - `riskWarnings`
  - `missingInformation`
- 模型输出中不能引用不存在的来源 ID
- 前端可点击查看至少一种证据来源

### 6. 补齐“任务已落库但未投递”补偿链路

**目标**

- 解决 `MySQL 已写成功，但 XADD 前崩溃` 的缺口
- MVP 采用“投递标记 + 定时修复扫描”方案

**原因**

- 当前补偿主要覆盖 Stream 之后的失败
- 还没有覆盖投递前崩溃场景

**验收条件**

- 任务表具备可区分“已创建但未成功投递”的状态或标记
- 定时扫描器能重新投递漏投任务
- 存在针对该场景的自动化测试或可重复演示脚本
- 重复投递不会造成重复切片

### 7. 安全治理补齐到排障场景（已完成）

**目标**

- 文档提示注入防护
- 日志脱敏
- 工具参数越权防护
- 白名单和只读凭证约束

**原因**

- 当前鉴权和 userId 过滤是有的，但还不够支撑排障场景

**验收条件**

- 知识检索结果、日志结果、工具结果统一标记为不可信输入
- 日志脱敏覆盖 Token、Cookie、密码、手机号、邮箱、连接串、Secret
- 高风险字段只能由服务端注入
- 存在至少一条注入攻击样例测试

### 8. 建立最小排障评测集（已完成）

**目标**

- 在开发阶段前置排障场景评测

**原因**

- 目标架构要证明效果，而不是只证明“用了什么技术”

**验收条件**

- 至少覆盖四类用例：
  - SOP 问答
  - 错误码召回
  - 状态+日志联合排查
  - 权限越权防护
- 输出至少四类指标：
  - Recall@K
  - 精确 Token 命中率
  - 引用准确率/覆盖率
  - 工具选择准确率

---

## P1

### 9. 文档版本化与删除治理

**目标**

- 文档更新带 `doc_version`
- 删除旧版本向量或做失效标记

**原因**

- 当前文档切片治理还偏“按 taskId 清理”
- 目标架构需要更明确的版本语义

**验收条件**

- 入库链路显式生成版本号
- 检索结果不会混入过期版本
- 删除或更新后不会残留旧切片脏数据

### 10. 知识沉淀改为排障卡片草稿闭环

**目标**

- 将高价值排查结论沉淀为知识卡片草稿
- 由人工确认后入库

**原因**

- 当前知识卡片体系已经存在，但还没有收敛到排障闭环

**验收条件**

- 至少支持从一次排障结果生成草稿卡片
- 草稿包含故障现象、原因、步骤、引用证据
- 人工确认后进入目标知识库

### 11. `ticket_query_tool` 接入

**目标**

- 在知识检索、状态查询、日志查询之外，补一条历史工单查询路径

**原因**

- 排障经验中，历史工单是重要证据来源

**验收条件**

- 工具支持按工单编号、状态、负责人等结构化查询
- 至少一条场景中，Agent 能结合工单结果调整排查步骤

### 12. Trace 页面与回放视图增强

**目标**

- 把现有 trace 从通用对话 trace，增强为排障执行回放

**原因**

- 目标架构强调可追溯与可解释

**验收条件**

- 页面能展示：
  - 路由结果
  - 检索 query
  - 召回分数
  - 工具参数
  - 工具结果摘要
  - 最终 Evidence 引用

### 13. 失败注入与降级演示

**目标**

- 补齐面向答辩的故障注入演示材料

**原因**

- 当前有 Resilience4j，但缺少排障目标态下的演示闭环

**验收条件**

- 至少演示：
  - 工具超时降级
  - 检索腿失败后的单腿回退
  - SSE 异常中断后的资源释放

---

## P2

### 14. 完整 RBAC 演进

**目标**

- 从轻量 workspace 模型演进到 tenant/team/role

**验收条件**

- 权限模型设计文档明确
- 至少完成表结构与服务端授权扩展方案

### 15. OCR 是否保留的重新决策

**目标**

- 只在目标数据集确实存在扫描件时保留 OCR

**验收条件**

- 演示数据统计中明确扫描件占比
- 若占比低，则在目标 MVP 中降级为可选模块

### 16. 现有 Rerank 是否保留的评测决策

**目标**

- 通过评测决定是否继续保留当前 rerank 能力

**验收条件**

- 至少一组有无 rerank 的效果与成本对比
- 若收益不明显，目标 MVP 不默认依赖 rerank

### 17. 通知/工单草稿工具

**目标**

- 在排障闭环后生成通知草稿、工单草稿

**验收条件**

- 仅生成草稿，不执行真实发送或创建
- 草稿内容带引用证据和风险提示

---

## 建议执行顺序

建议按以下顺序推进：

1. Milvus 检索迁移
2. Query Router
3. 排障专用工具
4. `workspace + visibility`
5. Evidence + 结构化回答
6. 投递补偿与安全治理
7. 最小评测集
8. 卡片闭环与工单能力

---

## 路线图结论

这份路线图的重点不是继续扩功能，而是把当前通用 Agent 代码库收敛成一个目标清晰、边界明确、证据可追踪、权限可答辩的排障辅助系统。

只要 `P0` 完成，目标架构就基本成立；`P1` 决定项目是否足够完整、足够像一个真实排障产品；`P2` 再决定它能否进一步平台化。
